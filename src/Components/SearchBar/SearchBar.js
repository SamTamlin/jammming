import React from "react";

import SearchButton from "../Buttons/Search/SearchButton";

const SearchBar = () => {
    return (
        <div>
            <input></input>
            <SearchButton />
        </div>
    );
}

export default SearchBar;